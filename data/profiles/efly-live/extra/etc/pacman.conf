[options]
HoldPkg     = pacman glibc
Architecture = auto
Color
SigLevel    = Required DatabaseOptional
LocalFileSigLevel = Optional
IgnorePkg=linux # prevent kernel updates

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[community]
Include = /etc/pacman.d/mirrorlist

# uncomment to use ymerge packages from pacman
# run "ymerge --sync" once first, to make sure the package db exists
#[curated-aur]
#Server = file:///var/lib/ymerge/repo/curated-aur/pkg
